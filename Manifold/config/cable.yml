development:
  adapter: async

test:
  adapter: test

production:
  adapter: redis
  # O Rails busca a URL que o Jenkins injetou (redis://redis:6379/1)
  url: <%= ENV.fetch("REDIS_URL") { "redis://localhost:6379/1" } %>
  channel_prefix: manifold_production

